"""Admin user-management module (Phase 5).

Admin-only endpoints to list/create/update users, activate/deactivate them,
assign roles, and list roles. All endpoints require the `admin` role. No public
registration endpoint exists.
"""
