# NeuroBridge backend dependencies
# ---------------------------------------------------------------------------
# Install inside a virtual environment (run from the backend/ folder):
#
#     py -m venv .venv
#     .venv\Scripts\activate       (Windows)   |   source .venv/bin/activate (Unix)
#     python -m pip install -r requirements.txt
#
# The "Active" section is what Phase 2 (Backend Skeleton) needs and installs now.
# It is intentionally pure-Python / wheel-only so it installs without a C/C++
# compiler. Compiled or heavier dependencies are deferred to the phase that needs
# them (see "Later phases").
# ---------------------------------------------------------------------------

# ============================ Active (Phase 2) =============================

# Web framework / ASGI server (plain uvicorn = no compiled extras required)
fastapi>=0.110
uvicorn>=0.29

# Settings / validation / .env loading
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# Testing
pytest>=8.0
httpx>=0.27

# ========================= Later phases (disabled) =========================

# Database ORM + migrations + PostgreSQL driver (enable in Phase 3).
# NOTE: On Windows + Python 3.9, SQLAlchemy pulls in `greenlet`, which needs the
# MSVC C++ Build Tools to compile. Prefer Python 3.11+ (has prebuilt wheels) or
# install the "Microsoft C++ Build Tools" before enabling these.
# SQLAlchemy>=2.0
# alembic>=1.13
# psycopg2-binary>=2.9

# Auth / security (enable in Phase 4)
# python-jose[cryptography]>=3.3
# passlib[bcrypt]>=1.7
# python-multipart>=0.0.9
