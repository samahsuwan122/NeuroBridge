# NeuroBridge backend dependencies
# ---------------------------------------------------------------------------
# These are the PLANNED dependencies for the FastAPI backend. They are installed
# in Phase 2 (Backend Skeleton) inside a virtual environment:
#
#     py -m venv .venv
#     .venv\Scripts\activate      (Windows)   |   source .venv/bin/activate (Unix)
#     pip install -r requirements.txt
#
# Nothing is imported/used yet in Phase 1 (foundation only). Versions may be
# pinned more strictly as phases progress.
# ---------------------------------------------------------------------------

# --- Web framework / server ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Settings / validation ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# --- Database / migrations (Phase 3) ---
SQLAlchemy>=2.0
alembic>=1.13
# PostgreSQL driver (official database). SQLite needs no extra driver.
psycopg2-binary>=2.9

# --- Auth / security (Phase 4) ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.9

# --- Testing (Phase 2) ---
pytest>=8.0
httpx>=0.27
